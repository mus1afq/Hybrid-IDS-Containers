# Hybrid IDS FYP — core runtime dependencies.
#
# Install with:
#   pip install -r requirements.txt
#
# Pins use compatible-release operators (~=) to allow patch updates while
# keeping the major and minor lines stable. The pins were chosen to match the
# versions that the dissertation evaluation was run against and are known to
# work together.
#
# Optional extras are intentionally kept out of this file. See:
#   requirements-dev.txt              -> pytest, lint tooling (used by CI)
#   requirements-extras-training.txt  -> xgboost, lightgbm (model sweep only)
#
# System-level tools used by the live capture pipeline are not pip packages.
# Install them from the OS package manager:
#   - Docker Desktop  https://www.docker.com/products/docker-desktop
#   - tshark          apt-get install tshark / brew install wireshark
#   - sysdig          via the docker/sysdig image; see run_and_capture.sh

# ---- Data and numerics ----
numpy~=1.26
pandas~=2.2

# ---- Modelling ----
scikit-learn~=1.4
joblib~=1.4

# ---- Plot generation for the dissertation figures ----
matplotlib~=3.8
seaborn~=0.13
